# Breaking Changes

A breaking change modifies existing behavior in a non-backward-compatible way.

Examples:
- Removing endpoints
- Renaming public APIs
- Modifying response contracts
- Deleting database columns
- Changing authentication flows

## Syntax

Header:
feat(api)!: remove v1 authentication endpoint

Footer:
BREAKING CHANGE: removed deprecated v1 login endpoint
