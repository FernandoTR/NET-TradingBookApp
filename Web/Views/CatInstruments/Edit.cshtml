@model Web.Models.CatInstrumentsViewModels

@{
    Layout = null;
}

@using (Html.BeginForm("Update", "CatInstruments", FormMethod.Post, new { id = "frmdata" }))
{
    @Html.AntiForgeryToken()

    <!--begin::Input group-->
    <div class="d-flex flex-column mb-8 fv-row">
        @Html.LabelFor(d => d.Name, new { @class = "d-flex align-items-center fs-6 fw-bold mb-2 required" })
        @Html.TextBoxFor(d => d.Name, new { @class = "form-control form-control-solid" })
    </div>
    <!--end::Input group-->
    <!--begin::Input group-->
    <div class="d-flex flex-column mb-8 fv-row">
        @Html.LabelFor(d => d.Ticker, new { @class = "d-flex align-items-center fs-6 fw-bold mb-2 required" })
        @Html.TextBoxFor(d => d.Ticker, new { @class = "form-control form-control-solid" })
    </div>
    <!--end::Input group-->
    <!--begin::Input group-->
    <div class="d-flex flex-column mb-8 fv-row">
        @Html.LabelFor(d => d.InstrumentType, new { @class = "d-flex align-items-center fs-6 fw-bold mb-2 required" })
        @Html.TextBoxFor(d => d.InstrumentType, new { @class = "form-control form-control-solid" })
    </div>
    <!--end::Input group-->
    <!--begin::Input group-->
    <div class="d-flex flex-column mb-8 fv-row">
        @Html.LabelFor(d => d.Currency, new { @class = "d-flex align-items-center fs-6 fw-bold mb-2 required" })
        @Html.TextBoxFor(d => d.Currency, new { @class = "form-control form-control-solid" })
    </div>
    <!--end::Input group-->
    <!--begin::Input group-->
    <div class="d-flex flex-column mb-8 fv-row">
        @Html.LabelFor(d => d.Market, new { @class = "d-flex align-items-center fs-6 fw-bold mb-2 required" })
        @Html.TextBoxFor(d => d.Market, new { @class = "form-control form-control-solid" })
    </div>
    <!--end::Input group-->
    <!--begin::Input group-->
    <div class="d-flex flex-column mb-8 fv-row">
        @Html.LabelFor(d => d.LinkIcon, new { @class = "d-flex align-items-center fs-6 fw-bold mb-2 required" })
        @Html.TextBoxFor(d => d.LinkIcon, new { @class = "form-control form-control-solid" })
    </div>
    <!--end::Input group-->
    <!--begin::Actions-->
    <div class="text-center">
        <button type="button" id="kt_modal_new_target_cancel" class="btn btn-light me-3" data-bs-dismiss="modal">Cancelar</button>
        <button type="button" id="btnSave" class="btn btn-primary">
            <span class="indicator-label">Guardar</span>
            <span class="indicator-progress">
                Espere por favor...
                <span class="spinner-border spinner-border-sm align-middle ms-2"></span>
            </span>
        </button>
    </div>
    <!--end::Actions-->
    /* campos para validaciones */
    <div hidden>
        @Html.HiddenFor(d => d.Id)
        @Html.HiddenFor(d => d.IsActived)
    </div>
}
<script type="text/javascript">
     $(document).ready(function() {
            // Define form element
            let form = document.getElementById('frmdata');

            // Init form validation rules.
            var validator = FormValidation.formValidation(
                form,
                {
                    fields: {
                        'Name': {
                            validators: {
                                notEmpty: {
                                    message: 'El nombre es obligatorio'
                                },
                                stringLength: {
                                    min: 1,
                                    max: 100,
                                    message: 'Debe tener entre 1 y 100 caracteres'
                                },
                            }
                        },
                        'Ticker': {
                            validators: {
                                notEmpty: {
                                    message: 'La ticker es obligatorio'
                                },
                                stringLength: {
                                    min: 1,
                                    max: 20,
                                    message: 'Debe tener entre 1 y 20 caracteres'
                                }
                            }
                        },
                        'InstrumentType': {
                            validators: {
                                notEmpty: {
                                    message: 'La tipo de instrumento es obligatorio'
                                },
                                stringLength: {
                                    min: 1,
                                    max: 20,
                                    message: 'Debe tener entre 1 y 20 caracteres'
                                }
                            }
                        },
                        'Currency': {
                            validators: {
                                notEmpty: {
                                    message: 'La moneda es obligatoria'
                                },
                                stringLength: {
                                    min: 1,
                                    max: 10,
                                    message: 'Debe tener entre 1 y 10 caracteres'
                                }
                            }
                        },
                        'Market': {
                            validators: {
                                notEmpty: {
                                    message: 'El mercado es obligatorio'
                                },
                                stringLength: {
                                    min: 1,
                                    max: 50,
                                    message: 'Debe tener entre 1 y 50 caracteres'
                                }
                            }
                        },
                    },
                    plugins: {
                        trigger: new FormValidation.plugins.Trigger(),
                        bootstrap: new FormValidation.plugins.Bootstrap5({
                            rowSelector: '.fv-row',
                            eleInvalidClass: '',
                            eleValidClass: ''
                        }),
                    }

                }
            );

            // Submit button handler
            const submitButton = document.getElementById('btnSave');
            submitButton.addEventListener('click', function (e) {
                // Prevent default button action
                e.preventDefault();

                // Validate form before submit
                if (validator) {
                    validator.validate().then(function (status) {
                        if (status == 'Valid') {
                            // Show loading indication
                            submitButton.setAttribute('data-kt-indicator', 'on');

                            // Disable button to avoid multiple click
                            submitButton.disabled = true;

                            form.submit(); // Submit form
                        }
                    });
                }
            });



    });
</script>